export function burst(text='CHAOS!'){const el=document.createElement('div');el.className='fx-burst';el.innerHTML=`<span>${text}</span>`;document.body.appendChild(el);for(let i=0;i<18;i++){const p=document.createElement('i');p.style.setProperty('--a',`${i*20}deg`);p.style.setProperty('--d',`${70+Math.random()*120}px`);p.textContent=i%3===0?'✦':'•';el.appendChild(p)}setTimeout(()=>el.remove(),1100)}
export function screenShake(){document.documentElement.classList.remove('shake');void document.documentElement.offsetWidth;document.documentElement.classList.add('shake');setTimeout(()=>document.documentElement.classList.remove('shake'),420)}
export function confetti(){const wrap=document.createElement('div');wrap.className='confetti-field';for(let i=0;i<70;i++){const p=document.createElement('i');p.style.left=`${Math.random()*100}%`;p.style.animationDelay=`${Math.random()*1.2}s`;p.style.setProperty('--x',`${(Math.random()-.5)*180}px`);p.textContent=['✦','◆','●','✧'][i%4];wrap.appendChild(p)}document.body.appendChild(wrap);setTimeout(()=>wrap.remove(),2600)}